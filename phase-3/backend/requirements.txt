# Runtime dependencies ONLY (Vercel serverless bundle must stay under 225 MB).
# Dev/local extras (uvicorn, alembic, pytest, httpx...) live in requirements-dev.txt.

# FastAPI and dependencies
fastapi>=0.104.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
google-generativeai


# SQLModel for database operations
sqlmodel>=0.0.14

# Async PostgreSQL driver (MCP server async engine)
asyncpg>=0.29.0

# Database
psycopg2-binary>=2.9.9

# Environment configuration
python-dotenv>=1.0.0

# Authentication
bcrypt>=4.1.0
pyjwt>=2.9.0
python-jose>=3.3.0
passlib[bcrypt]>=1.7.4