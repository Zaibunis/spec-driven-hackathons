# Development / local-run dependencies.
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
# NOT installed on Vercel (keeps the serverless bundle under the size limit).

# Local ASGI server (Vercel runs the app without uvicorn)
uvicorn[standard]>=0.24.0

# Database migrations
alembic>=1.13.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# FastAPI form/file parsing (unused by the current API, keep for local parity)
python-multipart>=0.0.6
